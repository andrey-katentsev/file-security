[16 мая 2013]
Версия 0.9
- ...

РАЗРАБОТКА:
- progress : 0-100%
- шифрование на основе CryptProtectData
- перенос ключей, при смене папки хранилища (перенос хранилища ключей в новое месторасположение)
- перейти на KAA::filesystem::path
- добавить unit-тесты
- строка состояния + курсор
- отмена (откат) изменений (побочных эффектов) при сбое (отмене)
- увеличить быстродействие: размер блока (4096), генерация случайных данных
- сообщение о результатах шифрования
- активация/дективация кнопок, (одна кнопка?)
- self-signed certificate
- проверить все файлы (#include см. стандарт кодирования)
- добавить текстовое описание к исключениям (iopen_file :: "unable to open file %1")

- зависимости проектов
	- filesystem::path::directory - forward declaration
- SDK: просмотреть все модули и добавить заголовки к файлам, классам, функциям (SAFE GUARANTEE).
- масштабируемость : OpenMP

ТЕСТИРОВАНИЕ
- отделить kernel core (runtime) в отдельный проект (static library)
- для отладки Post-Build Event > Use In Build > No
- создание файла для хранения ключей
- in-memory filesystem
- UserSessionKeyFileCipher : читает файл целиком (проверить на максимальный размер файла)

НА БУДУЩЕЕ:
- using namespace KAA;
- заменить Unable на Failed.
- добавить в классы защиту от копирования
- переместить абстракции в KAA SDK
- добавить в файлы заголовки // КАА, 2014
- добавить C++11 код 
	- FUTURE: C++11: = delete
	- заменить std::auto_ptr на std::unique_ptr
	- заменить throw() на noexcept

[Linker :: General :: Output]
$(OutDir)\$(ProjectName).exe
$(OutDir)\$(ProjectName).dll

ГОТОВО:
- диалоги по стандарту MS :: Layout
